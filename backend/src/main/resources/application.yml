server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://root:uPMfhXPQpvRYFxqtJWvyvePUqgEWwulb@nozomi.proxy.rlwy.net:20535/railway}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:uPMfhXPQpvRYFxqtJWvyvePUqgEWwulb}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    defer-datasource-initialization: true
  
  sql:
    init:
      mode: always

jwt:
  secret: VietLawJWTSecretKey2024ForProductionUseWithMinimum256BitsSecurityCompliantRandomGeneratedString
  expiration: ${JWT_EXPIRATION:86400000}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:https://viet-law.vercel.app,http://localhost:5173}

gemini:
  api-key: ${GEMINI_API_KEY:AIzaSyDIf3qW0l0BVheLoCbPuKH-9ZVqxZBvlC0}
  model: ${GEMINI_MODEL:gemini-1.5-flash}
  max-tokens: ${GEMINI_MAX_TOKENS:2048}
  temperature: ${GEMINI_TEMPERATURE:0.7}
  
logging:
  level:
    com.vietlaw: INFO
    org.springframework.security: WARN
